============
Installation
============

At the command line::

    $ easy_install filertools

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv filertools
    $ pip install filertools
